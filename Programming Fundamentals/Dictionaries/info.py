# r = [i for i in range(10)]
# a = {i: i for i in range(10)}
# print(a)
# print(r)

# my_dict = {'Ivan': 1, 'Anton': 2, 'Gosho': 3}
# print(my_dict)

# sort_dict = dict(sorted(my_dict.items(), key=lambda tokens: tokens[1])) ascending order
# sort_dict = dict(sorted(my_dict.items(), key=lambda tokens: -tokens[1])) descending order
# sort_dict = dict(sorted(my_dict.items(), key=lambda tokens: (-tokens[1], tokens[0])))
# print(sort_dict)

# dct = {':': ['D', 'P', 'F']}
# length = dct[':']
# for i in range(0, len(dct[':'])):
#     print(':' + dct[':'][i])
#     print(':' + '\n'.join(dct[':']))

# dictionary = dict(sorted(dictionary.items(), key=lambda x: -len(x[1])))
# dictionary = {x: sorted(dictionary[x]) for x in dictionary.keys()}
